# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# iOS native build artifacts (CI regenerates the ios/ project each build)
ios/App/Pods/
ios/App/App/public/
ios/build/
ios/DerivedData/
DerivedData/
build/ios/
*.ipa
GoogleService-Info.plist

# The API server is a separate concern and holds secrets — not needed to build
# the mobile app (it talks to the deployed API). Keep the whole folder out.
server/

# Credentials, operational data exports, binaries, tooling — never in the app repo
PILOT_PINS.csv
*.csv
*.apk
*.aab
downloads/
files */
DA_Fuel_App*.html
.claude/
*.p12
*.mobileprovision

# Secrets — never commit (rotate immediately if they ever were)
.env
*.env
.env.*
server/.env
keystore/
android/keystore.properties
android/app/google-services.json
server/firebase-service-account.json
*.p8
*.keystore
*.jks
